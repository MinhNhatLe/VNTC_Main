@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService authorizationService

@{
    var result = await authorizationService.AuthorizeAsync(this.User, "ViewManageMenu");
}

@if (result.Succeeded)
{
    <a class="dropdown-item text-success" asp-action="Index" asp-controller="AdminCP" asp-area="AdminCP">
        <i class="fa fa-cogs" aria-hidden="true"></i> Quản trị hệ thống
    </a>
    @*<div class="dropdown">
        <a class="nav-link text-light" asp-action="Index" asp-controller="AdminCP" asp-area="AdminCP"> QUẢN TRỊ</a>
        <a class="btn btn-outline-danger dropdown-toggle" href="#"
           id="managedropdownmenu" data-toggle="dropdown">
            Manage
        </a>
        <div class="dropdown-menu" aria-labelledby="managedropdownmenu">
            <a target="_blank" class="dropdown-item" asp-action="Index" asp-controller="FileManager" asp-area="Files">
                Quản lý files
            </a>
        </div>
    </div>*@
}